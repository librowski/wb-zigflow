import type { NodeDataProperties } from '@workflowbuilder/sdk';

import type { JoinSchema } from './schema';

export const defaultPropertiesData: NodeDataProperties<JoinSchema> = {
  label: 'Join',
  description: '',
};
